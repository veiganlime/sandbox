import MovieSection from "./MovieSection";
import React from 'react';
const MetadataEditor = () => {
  return <MovieSection title="Metadata Editor">Metadata Form</MovieSection>;
};

export default MetadataEditor;